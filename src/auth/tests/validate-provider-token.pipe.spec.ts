import { ValidateProviderTokenPipe } from '../validate-provider-token.pipe';

describe('ValidateProviderTokenPipe', () => {
  it('should be defined', () => {
    // expect(new ValidateProviderTokenPipe()).toBeDefined();
  });
});
